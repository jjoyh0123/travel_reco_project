<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="notice">

  <select id="totalCount" resultType="int">
    SELECT COUNT(*) FROM notice_table;
  </select>

  <select id="list" resultType="mybatis.vo.NoticeVO" parameterType="Map">
    SELECT * FROM (
    SELECT @RN := @RN + 1 AS rnum, a.*
    FROM (
    SELECT * FROM notice_table
    ORDER BY idx
    ) a, (SELECT @RN := 0) b
    ) c
    WHERE c.rnum BETWEEN #{begin} AND #{end}
  </select>

  <select id="getNotice" resultType="mybatis.vo.NoticeVO">
    SELECT * FROM notice_table
    WHERE status = 0
    ORDER BY idx desc
  </select>

  <select id="getFaq" resultType="mybatis.vo.FaqVO">
    SELECT * FROM faq_table
    WHERE status = 0
    ORDER BY idx desc
  </select>
</mapper>