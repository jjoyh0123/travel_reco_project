<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="supportSave">

  <insert id="customerSupport" parameterType="mybatis.vo.SupportVO">
    INSERT INTO support (user_idx, title, content, type, reg_date, status)
    VALUES (#{user_idx}, #{title}, #{content}, #{type}, NOW(), #{status})
  </insert>
</mapper>